<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

    <mapper namespace="member">


    <select id="selectAll" resultType="memberVO">
      SELECT * FROM sieunTest
    </select>

    <select id="get" resultType="string">
      SELECT memberPw FROM sieunTest WHERE memberName = #{memberName}
    </select>


    <insert id="insert">
        <selectKey keyProperty="num" resultType="int">
            SELECT last_insert_id()
        </selectKey>
        INSERT INTO sieunTest (num,memberName, memberPw)
        VALUES (LAST_INSERT_ID(), #{memberName}, #{memberPw} )
    </insert>


    <update id="update">
      UPDATE sieunTest
      SET memberName =#{memberName}, memberPw =#{memberPw}
      WHERE num = #{num}
    </update>

    <update id="patch">
        UPDATE sieunTest
        SET membername =#{memberName}
        WHERE num =#{num}
    </update>

    <delete id="delete">
      DELETE FROM sieunTest WHERE num =#{num}
    </delete>




</mapper>